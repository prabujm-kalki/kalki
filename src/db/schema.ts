// Domain schemas are intentionally introduced in controlled stages.
// Authentication schema is generated/managed with Better Auth + Drizzle.
// Business-domain tables begin only after the Stage 3 environment gate passes.
export {};
